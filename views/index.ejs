<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contacts</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <main id="card">
        <div id="input-section">
            <div id="input-bar">
                <p id="heading-bar">Add your favourite contacts</p>
                <form action="/create-contact" method="POST">
                    <div>
                        <label>
                            Name:
                        </label>
                        <input type="text" name="name" id="itemInput" placeholder="Enter Name" required>
                    </div>
                    <div>
                        <label>
                            Phone No:
                        </label>
                        <input type="tel" name="phone" id="itemInput" placeholder="Enter Number" required>
                    </div>
                    <button class="add-btn">Add Contact</button>
                </form>

            </div>
        </div>
        <div id="item-list-division">
            <ul id="item-list">
                <!-- <li class="list-item">
                    <div class="contact-details">
                        <p class="name-view">Arnav Choudhary</p>
                        <p class="phone-view">7042881200</p>
                    </div>
                    <i class="fas fa-trash delete"></i>
                </li> -->
                <% for(const it of contact_list ) {%>
                <li class="list-item">
                    <div class="contact-details">
                        <p class="name-view"><%=it.name%></p>
                        <p class="phone-view"><%=it.phone%></p>
                    </div>
                    <a class="delete" href="/delete-contact/?id=<%=it._id%>">
                        <i class="fas fa-trash delete"></i>
                    </a>
                </li>
                <%}%>
            </ul>
        </div>
    </main>


    <script src="https://kit.fontawesome.com/c2e4f2d141.js" crossorigin="anonymous"></script>
</body>

</html>